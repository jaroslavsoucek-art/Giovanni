# Workflow catalog (NOT a ranking — see WORKFLOW_FITNESS.md).
# MVP: directory of declared workflows per node + observed reuse. No fitness score yet.

version: 0
updated: 2026-06-22

workflows:
  # - name: neo-digest
  #   node: shoptet-neo
  #   purpose: daily multi-source digest with drift detection
  #   observed_reuse: []        # nodes that forked/adopted it
  #   wired_to_shared: false    # explicit edges to org-shared artifacts?
  []
