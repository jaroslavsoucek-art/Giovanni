# Knowledge Index

<!--
This file is AUTO-GENERATED by `scripts/build-knowledge-index.sh` on every
Edit/Write to any file in `knowledge/` (except INDEX.md itself), via the
`post-knowledge-edit.sh` hook.

‼ Never edit by hand — manual edits get overwritten. To change format,
edit `scripts/build-knowledge-index.sh`, then re-regen.

The template below describes the target shape the script produces.
-->

> Auto-generated by `scripts/build-knowledge-index.sh` — refreshed on every `Edit`/`Write` to `knowledge/` via `.claude/hooks/post-knowledge-edit.sh`. **Manual edits to this file will be overwritten on the next knowledge change.**
>
> Generated: YYYY-MM-DD HH:MM UTC  ·  Last commit: `<hash>`

## Top-level docs

<!--
One row per `knowledge/*.md` file at depth 1, excluding INDEX.md and
README.md. Sorted alphabetically. One-liner extracted from the first H1
in the file (or first non-blank line if no H1).
-->

| File | One-line | Last modified | Size |
|---|---|---|---|
| [`<file>.md`](<file>.md) | `<one_liner_from_first_h1>` | YYYY-MM-DD | `<size>` |
| [`<file>.md`](<file>.md) | `<one_liner>` | YYYY-MM-DD | `<size>` |

## Data files (non-markdown)

<!--
Non-markdown files at `knowledge/` depth 1 — xlsx, html, pdf, csv, etc.
Section omitted if no such files exist.
-->

| File | Last modified | Size |
|---|---|---|
| [`<file>.xlsx`](<file>.xlsx) | YYYY-MM-DD | `<size>` |

## Archive

<!--
Only shown if `knowledge/archive/` exists. Count-only — superseded docs
are not browse-friendly, only audit-friendly. Section omitted if archive
directory is empty.
-->

[archive/](archive/) — `<N>` superseded docs (not indexed; explore by hand if you need historical context).

## Cross-references

<!--
Static footer hard-coded in the regen script. Points at:
  - The constitution (single source of truth)
  - Memory layer
  - Claude instructions (agent behaviour rules)
  - Custom agents directory (if exists)

The script knows the constitution filename via governance config — see
`docs/governance.config.template.yaml`.
-->

- **Living constitution:** [`<constitution>.md`](<constitution>.md) — single source of truth, supersedes any contradicting doc
- **Working memory:** [`../memory/`](../memory/) — stakeholders, briefs, decisions, audits, [optional: branch-out, shadow, calibration, intel, watch]
- **Claude instructions:** [`../CLAUDE.md`](../CLAUDE.md) — agent behaviour rules
- **Custom agents:** [`../.claude/agents/`](../.claude/agents/) — `<list of agent names>`
