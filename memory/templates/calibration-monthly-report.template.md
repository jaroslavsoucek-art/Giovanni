---
# ============================================================================
# Monthly calibration report — Layer 3 (memory/calibration/monthly/<YYYY-MM>.md)
# ============================================================================
# Generated at the start of each month by /calibration-report, summarizing
# shadow hypothesis accuracy from the previous month.
#
# Frontmatter optional but recommended for filtering / search.
# ============================================================================

month: YYYY-MM
# The month being reported on (NOT the date the report was generated).

generated_at: YYYY-MM-DD
# Generation date (typically 1st of the following month).

total_hypotheses_generated: <integer>
# All shadow hypotheses with `created` date in this month.

resolved: <integer>
# Status resolved-yes + resolved-no + resolved-mixed.

matched_with_caveat: <integer>
# Subset of resolved-yes whose `caveat:` field is non-empty — substance
# matched, channel/timing missed. Counts as matched in accuracy, tracked
# separately to keep verdict generosity visible.

expired_without_verdict: <integer>
# Status expired (past horizon_at, no signal, no human verdict).

awaiting_resolution: <integer>
# Status pending whose horizon_at lands in next month.

overall_accuracy: <float between 0.0 and 1.0>
# matched / (matched + falsified). Excludes mixed (or counts mixed as falsified
# per binding rule — see governance section).

specificity_gate_rejections: <integer>
# Hypotheses rejected at generation by the specificity_gate. Health signal —
# stable or growing = good (discipline), declining = soft predictions slipping through.

actors_flagged_for_enrichment: []
# List of actor slugs whose profile depth was repeatedly insufficient this month.

triage_threshold_suggestions: []
# Suggestions for /triage-heuristic.yaml threshold review. Agent SUGGESTS;
# user applies.

---

# Calibration report — <YYYY-MM>

<!--
This report aggregates shadow hypothesis accuracy for ONE MONTH. It rolls up
into a quarterly /shadow-review for governance audit.

Length: 80-200 lines. Shorter = not enough signal extracted; longer = over-
reporting routine numbers that the YAML already captures.
-->

**Month:** YYYY-MM
**Generated:** YYYY-MM-DD
**Generated by:** `/calibration-report`

---

## Volumes

<!--
The raw counts. These flow from the canonical YAML state file
memory/calibration/actor-scores.yaml and per-shadow-hypothesis verdict files.
-->

| Metric | Count |
|--------|-------|
| Shadow hypotheses generated this month | <N> |
| Rejected at generation by specificity_gate | <N> |
| With testable outcome (matched + falsified) | <N> |
| Matched strict (substance + channel + timing) | <N> |
| Matched with caveat (substance hit; channel/timing missed) | <N> |
| Resolved-mixed (partial match) | <N> |
| Expired without ground truth | <N> |
| Awaiting resolution (horizon spills into next month) | <N> |

<!--
Strict vs caveat split: matched-with-caveat counts as matched in
overall_accuracy and actor scores (channel/timing miss alone is not
falsified), but the split is reported so a creeping caveat share is
visible. If caveat matches exceed ~1/3 of all matches, expected_signal
channel specificity is drifting — tighten the templates rather than
keep absorbing the misses.
-->


**Triage volume health:**
- Active branch-out runs this month: <N> (against `triage-heuristic.yaml` `branch_out_eligibility.daily_max`)
- Shadow generation peak day: <N> (against `shadow_generation_eligibility.daily_max`)
- Hard-fail breaches (shadow > daily hard-fail max): <N> (target: 0)

## Overall accuracy

<!--
Single roll-up number. WARN if > 80%: that means agent is sandbagging
predictions (only tier-likely on safe bets) and /shadow-review should be
triggered immediately (governance failure mode #4 in constitution).
-->

- **Current month:** <X.X>% (only over resolved testable outcomes; n=<N>)
- **Previous month:** <X.X>% (n=<N>)
- **Trend:** <improving | stable | degrading>

<!--
GUARD RAIL: if overall_accuracy > 0.80, surface the following callout in
the report. The user is supposed to immediately trigger /shadow-review to
audit motivated-reasoning patterns in adversarial-check verdicts.
-->

**⚠ Accuracy > 80% — run `/shadow-review` immediately.** High accuracy is suspect, not celebratory. Either tier labels have drifted (predictions are no longer testing what they should), or adversarial-check verdicts are motivated. Quarterly audit cadence should be moved up.

## Per-actor scores

<!--
One row per actor with ≥1 resolved hypothesis this month.
Sort by total n descending, then by accuracy ascending (struggling actors
surface first).

Columns:
- Actor slug
- n (resolved hypotheses count this month)
- Accuracy (matched / resolved)
- Trend vs. last month (improving / stable / degrading / new)
- Bias-watch flags from per-actor calibration record (if any)
-->

| Actor | n | Accuracy | Trend | Bias-watch flags |
|-------|---|----------|-------|------------------|
| `<slug-1>` | <N> | <X.X>% | <trend> | <flag-1>, <flag-2> |
| `<slug-2>` | <N> | <X.X>% | <trend> | — |
| `<slug-3>` | <N> | <X.X>% | <trend> | <flag> |

## Per-tier calibration

<!--
The three-tier framing is the IP. Calibration across tiers should look like:
- likely: 60-80% hit rate
- possible-but-surprising: 20-40% hit rate
- unlikely-but-impactful: 5-15% hit rate

Deviation signals tier-label drift. If likely is at 95%, the agent is
sandbagging (only making bets it knows it can win). If unlikely is at 30%,
the agent isn't labeling correctly (calling things "unlikely" that are
actually likely).
-->

| Tier | n | Matched | Hit rate | Healthy range | Flag |
|------|---|---------|----------|---------------|------|
| `likely` | <N> | <N> | <X.X>% | 60-80% | <ok / sandbagging / over-confident> |
| `possible-but-surprising` | <N> | <N> | <X.X>% | 20-40% | <ok / mis-labeled> |
| `unlikely-but-impactful` | <N> | <N> | <X.X>% | 5-15% | <ok / mis-labeled> |

## Specificity gate health

<!--
The specificity_gate in triage-heuristic.yaml rejects vague predictions at
generation time. Its health is measured by:
- rejected count (stable or growing = discipline holding)
- accepted-but-expired count (declining = better specificity over time)

If accepted-but-expired is climbing, predictions are getting through that
aren't actually testable.
-->

- **Rejected by gate this month:** <N> (previous month: <N>) — <stable / growing / declining>
- **Accepted but expired without ground truth:** <N> (previous month: <N>) — <stable / growing / declining>
- **Specificity health:** <ok / drift / urgent — re-tighten specificity_gate>

## Observation flags

<!--
Patterns the agent surfaces for user attention. These are SUGGESTIONS,
not changes — agent never modifies triage-heuristic.yaml or stakeholder
profiles itself. User applies suggestions manually.

Common patterns to surface:
- "Agent systematically under-predicts adversarial moves from <actor>" — re-read profile
- "Tier-likely predictions for <actor> hit 50% — re-calibrate this actor"
- "Channel-specificity miss rate is up to 30%" — review expected_signal templates
- "<actor> has 8 predictions / 2 hits this month — depth gap, re-bootstrap profile"
- "<topic-area> predictions miss at 60% — possibly an actor-domain mismatch"
-->

### Bias patterns

- **<pattern>** — <observation citing case IDs>. Suggestion: <what to do>.
- **<pattern>** — <observation>. Suggestion: <what>.

### Profile enrichment recommendations

<!--
Actors whose profile depth was insufficient this month. Target: top 7 list
of actors most worth deepening.

Format:
- **`<slug>`** — <total hypotheses>: <hits>/<resolved> = <X.X>%. <Reason — e.g.
  "shallow profile depth, predicted reactions section is 1 entry">
-->

- **`<slug>`** — <n hypotheses>: <X.X>% accuracy. <Reason>.
- **`<slug>`** — <n hypotheses>: <X.X>% accuracy. <Reason>.

### Triage threshold suggestions

<!--
Recommendations for /memory/triage-heuristic.yaml changes. Agent surfaces;
user applies via manual edit + commit.

Format:
- **<heuristic key>:** current <value> → suggested <value>. Reason: <data>.
-->

- **`shadow_generation_eligibility.daily_max`:** current 15 → suggested 12. Reason: peak day this month hit 18, no signal-quality loss observed in cohort below 12.
- **`specificity_gate.expected_signal_search_terms_min`:** current 2 → suggested 3. Reason: accepted-but-expired rate ticked up; tighter gate would have caught 4 of 7 expirations.

## Notable cases

<!--
3-5 most instructive shadow hypotheses from this month. Each:
- ID (anonymized if helpful, but keep traceable)
- 1-line topic
- Outcome (matched / falsified / expired)
- What the case teaches about calibration

Anti-pattern: just listing every case. Pick the cases that have transferable
learning.
-->

### <Case 1>: <one-line topic>

- **ID:** `<shadow-id>`
- **Outcome:** matched | falsified | mixed | expired
- **Lesson:** <what this case teaches about calibration patterns>

### <Case 2>: <topic>

- **ID:** `<shadow-id>`
- **Outcome:** <verdict>
- **Lesson:** <transferable learning>

## Governance posture

<!--
Has anything in the governance layer (triage-heuristic, stakeholder profile
depths, shadow review cadence) broken or showing strain?

This section feeds the quarterly /shadow-review.
-->

- **Triage volume:** <ok / strained / breached>. <Explanation>.
- **Profile-depth sufficiency:** <ok / drift>. <Explanation citing flagged actors>.
- **Specificity discipline:** <holding / drifting>. <Explanation>.
- **Adversarial-check integrity:** <intact / drift suspected — run /shadow-review>.

## Recommendations for principal

<!--
2-5 concrete next actions for the principal. These are the actionable
output of the report — what the principal should do as a result.
-->

1. **<Action>** — by <date or cadence>.
2. **<Action>** — by <date or cadence>.
3. **<Action>** — by <date or cadence>.

## Related artifacts

- **Canonical YAML state:** `memory/calibration/actor-scores.yaml`
- **Per-actor narrative records (if any):** `memory/calibration/actors/<slug>.md`
- **Triage heuristic:** `memory/triage-heuristic.yaml`
- **Previous month's report:** `memory/calibration/monthly/<YYYY-(MM-1)>.md`
- **Next /shadow-review window:** <YYYY-MM-DD> (quarterly cadence)
