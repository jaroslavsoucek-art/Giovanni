# Brief: <Event title> — YYYY-MM-DD HH:MM

<!--
Pre-meeting brief. Generated by the digest agent for events within next 48h
(see digest-architect for trigger mechanics).

Purpose: in a one-person-show context, nobody else prepares an agenda for the
principal. This brief lifts session-entry context from ~60% to ~95% — counterparty
state, what's in their head, the principal's positions with evidence, fallback
plans.

Retention: briefs stay committed as historical record. Useful for retrospective
("what did I go in with vs what we actually discussed").

Filename: memory/briefs/YYYY-MM-DD_<event-slug>.md
-->

> **Counterparty:** <names + slugs> | **Channel:** <Teams / in-person / phone / email thread>
> **Generated:** YYYY-MM-DD HH:MM (Tz)
> **Pointer:** <relative path to relevant shard> *(if the meeting touches an ongoing topic)*

<!--
Pointer is optional but high-value when the meeting is one step in a longer arc
covered by a topic shard. Lets the brief stay focused on this single event
while the shard owns the longer story.
-->

## Counterparty state

<!--
Per recipient (one block each if multiple):
- Last touch: when + what (1 line)
- Sentiment trajectory: how their tone / engagement has shifted over recent
  interactions. Direction matters more than current state (e.g. "cooling
  from supportive to neutral over 2 weeks" is more actionable than "neutral").
- Pattern observation: what they tend to do in this kind of conversation,
  based on prior behaviour. 1-2 lines.
- Active threads vs them: what's pending in both directions.

Source: pull from `memory/stakeholders/<slug>.md` profile + recent digest entries.
If profile is shallow / missing, flag explicitly ("profile: shallow, signals
proxy-only") and lean on most recent direct interactions.
-->

### <Counterparty 1 name>

- **Last touch:** YYYY-MM-DD — <what>
- **Sentiment trajectory:** <direction over last 2-3 interactions>
- **Pattern observation:** <what they do in this kind of conversation>
- **Active threads:** <what's pending from / to them>

### <Counterparty 2 name>

- **Last touch:** YYYY-MM-DD — <what>
- **Sentiment trajectory:** <direction>
- **Active threads:** <pending>

## Hot topics in their head

<!--
3-5 things currently occupying the counterparty's attention. Derived from:
- Recent communications they sent / received (digest sources)
- Their other active responsibilities visible to you
- Things they've previously flagged as priorities
- External pressure points (their boss's asks, market signals affecting their role)

Format: numbered list, 1-3 lines each. Include what's bothering them, what they're
waiting on, what framework they're using to think about it.

This section is the brief's highest-leverage input. Talking points downstream
will land or miss based on how accurately this models the counterparty's state.
-->

1. **<Topic>** — <what's at stake for them, what they're waiting on>
2. **<Topic>** — <what's at stake for them, what they're waiting on>
3. **<Topic>** — <what's at stake for them, what they're waiting on>

## Your talking points (3-5)

<!--
Concrete positions you want to land. Each one:
- The point itself (1 line, active voice)
- Goal (what outcome you're going for)
- Evidence (data / quotes / contract clauses backing the position)

Bad: "mention pricing"
Good: "Land €19.90 as launch anchor with explicit future-flexibility clause.
Goal: close pricing reasoning task without committing to permanent price.
Evidence: BC v2 shows 3.5% churn at €19.90; competitor benchmark range €18-29;
DK/SE will deviate via flexibility clause, no precedent issue."

3-5 talking points typical. Fewer = under-prepared; more = unrealistic to
land in one meeting, prune.
-->

1. **<Position>** — Goal: <outcome>. Evidence: <data / quote / clause>.
2. **<Position>** — Goal: <outcome>. Evidence: <data / quote / clause>.
3. **<Position>** — Goal: <outcome>. Evidence: <data / quote / clause>.

## Fallback positions

<!--
For each likely pushback path, the second / third line of defense. Format:
- **If counterparty pushes <X>:** <fallback response>
- **If counterparty escalates to <Y>:** <next move>
- **Red line:** <position that cannot be ceded> — <why>

Red lines are explicit. Without them, in-meeting drift can give up positions
the principal would have held if they'd thought about it offline.
-->

- **If <counterparty> pushes <X>:** <fallback>
- **If <counterparty> escalates to <Y>:** <next move>
- **Red line:** <position> — <why>

## Expected pushback

<!--
Different from fallbacks. Fallbacks are reactive plans; this is proactive
prediction of what they'll push on.

For each: what they'll say, why they'll say it, how to address.

This is the predictive layer of brief prep — what should be hard before the
meeting starts.
-->

- **They'll likely say:** <prediction>. **Because:** <their interest>. **Response:** <how to handle>.
- **They'll likely say:** <prediction>. **Because:** <their interest>. **Response:** <how to handle>.

## Open from last meeting

<!--
Carryover items that didn't close last time. Each: what was raised, what was
left open, who owns the next move.

If nothing carries over, write "n/a — clean slate". Don't fabricate carryovers.
-->

- <Item> (from <date>) — <state>
- <Item> (from <date>) — <state>

## Asks / decisions needed today

<!--
What you want to leave the meeting with. Concrete and minimal — 1-4 items.
Each one:
- The ask (specific, with deadline if relevant)
- Who needs to commit
- What "yes" looks like

If you can't articulate what success looks like, the meeting isn't ready.
Defer or rescope.
-->

- **<Ask>** — from <counterparty>. Success = <observable commitment>.
- **<Ask>** — from <counterparty>. Success = <observable commitment>.

## What would be a win

<!--
One sentence describing the best realistic outcome. Different from "asks" —
asks are the menu, win is the headline.

Example: "Counterparty commits to a concrete next-step action with a date,
unblocking the dependency chain the principal owns this quarter."

This forces the brief writer (or generator) to hold a clear theory of the
meeting, not just a list of items.
-->

<One-sentence win condition.>

## Predictive layer (optional)

<!--
If you run a predictive layer (see prediction-architect), reference relevant
artifacts:
- Shadow hypotheses tracking this counterparty's likely moves
- Branch-out simulations of meeting outcomes
- Decision drafts pending the meeting

Omit this section entirely if you don't run those layers.
-->

- **Shadow [<id>] horizon t+1 / t+2** — <hypothesis tracked>
- **Decision draft:** `memory/decisions/<...>` *(pending this meeting's outcome)*
- **Branch-out:** `memory/branch-out/<...>` *(scenarios for this meeting)*
